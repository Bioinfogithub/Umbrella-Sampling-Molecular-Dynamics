# Umbrella windows

This directory is intended for the independent umbrella windows.

A typical window contains:

```text
window-001/
├── conf.gro
├── umbrella.mdp
├── topol.top
├── index.ndx
└── ...
```

Large trajectories and generated GROMACS output are intentionally ignored by `.gitignore`.
